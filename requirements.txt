faster-whisper>=1.1.0
sentence-transformers>=3.0.0

# Optional (local-only) model and embedding libraries. Install only if you plan to run local LLMs or embeddings.
# Examples:
# gpt4all
# llama-cpp-python
# sentence-transformers
# ffmpeg-python